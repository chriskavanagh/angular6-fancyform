import { DropboxDirective } from './dropbox.directive';

describe('DropboxDirective', () => {
  it('should create an instance', () => {
    const directive = new DropboxDirective();
    expect(directive).toBeTruthy();
  });
});
